# Передача 12-1 — подсветка участка и плотность карты

СДЕЛАНО: усиление карты в светлой теме закрыто и замерено (контраст линии маршрута к локальному фону 1,289 → 1,832, ×1,42; тёмная не тронута, 1,293). Подсветка активного участка реализована и работает: ореол-путь под линией, пульс по opacity, статически более толстый штрих, увеличенные узел и код города у концов участка. Контраст текста шагов перемерен тем же методом и не сдвинулся: активный 8,185 тёмная / 7,492 светлая, неактивный 1,825 / 1,731. Escape 0 на 84 клетках. reduced-motion: пульса нет, усиление статично. Тесты 83, tsc, lint, build — зелёные.

ФАЙЛЫ: src/styles/landing/story.css · src/components/landing/Europe.tsx · .autopilot/landing-adaptive-engagement/shots/12/ (кадры + measure-12.js.txt + final-12.js.txt).

РЕШЕНИЯ: гейт усиления — медиазапрос ступени `l` + `vh-pin`, а НЕ `motion-ok` (иначе при reduced-motion на широком окне усиленное состояние пропадало бы); `.jhalo` базово гасится `fill: none; stroke: none`, поэтому на 320/375 всё замеренное совпало с дотасковым значением; светлая тема усилена по токенам карты, а не ослаблением вуали — вуаль стоит между картой и текстом шагов.

ТУПИКИ: критерий «на шагах 2 и 3 видно, что машина едет» НЕ закрыт, и это геометрия, а не сила подсветки. Замер по нарисованным путям: на шаге 2 у активного участка НОЛЬ видимых точек — линия целиком за карточкой сцены и за скримом колонки шагов; оба города участка (MUC, LYO) тоже за ними. Полоса зазора между скримом и карточкой (32 px) не показывает разницы: crMax 1,750 с подсветкой против 1,782 без неё в тёмной, 2,139 против 2,130 в светлой. Что подсветка работает — доказано снятием скрима: тот же участок 1,363 → 3,965 (тёмная) и 1,484 → 2,439 (светлая).

ДАЛЬШЕ: развилка владельцу — (а) сузить правый вылет скрима с 60 до ~24 px и растушёвку с 52 до ~20, открыв полосу карты в зазоре грида (текст остаётся на сплошной канве, но кромка градиента становится круче); (б) сдвинуть маршрут истории так, чтобы участки 2 и 3 проходили по свободным полосам кадра; (в) принять, что на шагах 2 и 3 движение несут пилюля участка и подсветка узлов, а сам участок читается на шагах 1 и 4. Пиксельное «до/после» на 320/375 против кадров таска 11 сравнить не удалось — те сняты на другой позиции прокрутки; вместо него структурная сверка computed-стилей.
