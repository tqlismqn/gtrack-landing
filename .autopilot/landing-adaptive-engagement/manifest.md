# Манифест требований

Источник: `2026-08-17-brief.md`. Строку из этого списка может снять **только пользователь**.

## Продукт — адаптив (Б8)

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R01 | «Шапка не помещается на 401–430 px (iPhone XR/11/12PM/14PM, Pixel) — бургер уезжает за край» | done | причина найдена в коде: `≤400px` даёт `cta-tiny`, а на 401–430 действует `≤767px` с длинным «коротким» вариантом | spec, истории 5–9 → таск 03, коммит `670ded1` |
| R02 | «Трёхступенчатый своп CTA не работает: в 6 локалях „короткий“ вариант не короче, в 3 — длиннее» | done | пересчёт по словарям: не короче в **8** локалях, строго длиннее в **5** — цифры брифа (6 и 3) не подтвердились, проверю в пикселях при сборке | spec, истории 10–14 → таск 03, коммит `670ded1` |
| R03 | «Пиннутый scrollytelling на `100vh` включается с 980 px → на iPad landscape низ недостижим» | done | механика найдена: компактный режим висит на `max-height: 760px`, а у iPad landscape высота 768 — правило не срабатывает | spec, истории 15–18 → таск 04, коммит `7dec130` |
| R04 | «Видео 2,5 МБ качается при каждой загрузке (`play()` на mount, а не по входу во вьюпорт)» | done | подтверждено в коде: `play()` в `useEffect` на mount, `IntersectionObserver` отсутствует; файл 2 521 594 байта | spec, истории 32–35 → таск 02, коммит `52a693d` |
| R05 | «SVG-карта Европы на 320 px: штрихи 0,56 px, узлы 0,9 px — нечитаема» | done | подтверждено: `viewBox 860×560`, узлы `r=2.4` и `r=5` | spec, истории 29–31 → таск 05, коммит `b1abf47` |
| R06 | «Плюс сведение 16 пиксельных порогов к четырём» | done | пересчёт: уникальных порогов **21**, не 16. Шкала: 430 / 768 / 980 / 1280 + две высотные точки | spec, истории 1–4 → таск 01, коммит `73d54e8` |
| R07 | «Приёмка задана заранее: скриншоты 320 / 375 / 430 / 768 / 1024 / 1440 × обе темы. Без них — правка вслепую» | done | **Приёмка переснята ЦЕЛИКОМ по финальному коду** (`shots/07-final/`): 29 кадров — 12 приёмочных fullPage на шести ширинах × две темы, 8 кадров доказательства `R33`, 2 кадра `D02`, 2 кадра 1440×600, 4 кадра режима без анимации, 1 кадр найденного дефекта. Прежний комплект устарел (снят до тасков 09–13) и оставлен записью прошлого состояния. Замер: 96 клеток, все чисты на метриках, которые умеют краснеть — прежние не умели (`D08`) | таск 07, пересъёмка 18.08 |

## Продукт — графика и удержание (дополнение 17.08)

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R08 | «давай еще использовать higsfied AI для графики» | done | пользователь: «подключись к нему… Install the CLI… Authenticate… Install the companion skills». Сделано 17.08: CLI 1.1.23 уже стоял, skills поставлены, вход есть — free plan, **36 кредитов**. Цены: картинка 1–7, 3D GLB 5, видео 8 | spec, истории 37–39 → таск 06, коммит `84f9688` |
| R09 | «сделаем 3д графику или какую-то штуку, которая заставит задержаться на сайте» | done | пользователь выбрал: оживить карту Европы; 3D-модель в hero **отклонена им же** в пользу лёгкого варианта. Связка: маршрут едет синхронно с 4 шагами «истории одного водителя» | spec, истории 19–20, 23–26; часть про 3D — «Вне рамок» → таск 06, коммит `84f9688` |

## Ограничения территории

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R10 | «Это не Lovable — правим напрямую через PR → squash merge → авто-деплой» | in-ticket | уточнено владельцем: показ на localhost → апрув → деплой | spec, история 40 → таск 07 |
| R11 | «Локалей 12, 271 ключ» | done | подтверждено: ru + en в `landing-i18n.ts`, 10 файлов в `landing-locales/` | spec, история 14 → таск 03, коммит `670ded1` |
| R12 | «Next.js 16 + Tailwind, Vercel» | in-ticket | подтверждено: next 16.0.10, react 19.2.1, tailwind 4 | spec, история 40 → таск 07 |

## Процесс — обкатка skill (предмет решения владельца)

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R13 | «Манифест требований. Держит ли он исходные слова так, что ничего не теряется к третьей переделке» | in-spec | закрывается отчётом фазы 8, не кодом | spec, «Покрытие манифеста» |
| R14 | «Документы. Что реально остаётся на диске: `brief.md`, `manifest.md`, `spec.md`, тикеты, `interfaces.md`. Насколько это лучше того, что мы пишем сейчас руками» | in-spec | то же | spec, «Покрытие манифеста» |
| R15 | «Цена. Прогнать `python3 ~/.claude/skills/autopilot/tools/measure-run.py <каталог проекта>` и посмотреть, во что обошёлся прогон и насколько выросли контексты» | in-spec | прогон инструмента — обязательный пункт фазы 8 | spec, «Покрытие манифеста» |
| R16 | «Что пересекается с нашим каноном… Понять, что autopilot добавляет сверх этого, а что дублирует» | in-spec | то же | spec, «Покрытие манифеста» |
| R17 | «Куда он лезет лишнего. Например, попытки писать в чужой репозиторий, собственная память проекта параллельно нашей KB, свой дашборд вместо Пульта» | in-spec | наблюдения собираются по ходу прогона; первое уже есть — skill завёл `CLAUDE.md` в репозитории лендинга параллельно нашей KB | spec, «Покрытие манифеста» |
| R18 | «Решение по итогам: взять целиком · взять фазы 1–3 (манифест/бриф/спека) в наш канон · отклонить с причиной» | in-spec | рекомендация с обоснованием — последний раздел отчёта | spec, «Покрытие манифеста» |

## Подразумеваемые (в брифе не сказаны, но без них результат неполон)

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R19i | *(подразумевается)* новая графика обязана пережить те же ширины 320–1440 и обе темы, что чинит Б8 | done | Пережила: escape-метрика 0 в 96 клетках (12 локалей × 8 ширин, живые вьюпорты, мир «минус 10» с ассертом в каждой клетке); тарифы 48 клеток нулей с воспроизведённой худшей колонкой 221,5; `motion-ok` подтверждён у всех 12 локалей на всех восьми ширинах. Единственный найденный дефект — `de`@320 в секции видео — к новой графике не относится и закрыт таском 14 (`D08`) | таск 07, пересъёмка 18.08 |
| R20i | *(подразумевается)* новая графика не должна вернуть проблему R04 — тяжёлый ассет на каждой загрузке | done | бюджет: новые ассеты ≤ 150 КБ | spec, история 36 → таск 02, коммит `52a693d`; замер: 0 новых ассетов, JS +525 Б |
| R21i | *(подразумевается)* анимация уважает `prefers-reduced-motion`; движение — не единственный носитель смысла | done | движок `motion-ok` в проекте уже есть, сцена встраивается в него | spec, история 25 → таск 06, коммит `84f9688` |
| R22i | *(подразумевается)* откуда берутся ассеты Higgsfield: аккаунт, оплата, ручная генерация или API | closed | закрыт ответом на R08: генерирую я сам через CLI, оффлайн, ассет кладётся в `public/`. Ключ живёт в системном хранилище CLI, в репозиторий не попадает | — |

## Продукт — второй раунд владельца (17.08, вечер, на показе localhost)

Поступило после того, как волна была почти закрыта. Статус `open` означал: требование
записано его словами и ждёт решения по волне, а не забыто.

**Решение владельца получено 17.08 на показе: «Карту сейчас → деплой → контент отдельной
волной».** Поэтому пять строк переведены в `deferred`, а `R32` — в текущую волну.

> **Что означает `deferred` в этом манифесте и чем он закрывается.** Требование НЕ снято —
> снять его может только владелец своими словами. `deferred` означает: он решил перенести
> работу, цитата записана, и строка **закрывается появлением в манифесте следующей волны**,
> а не строкой в отчёте. Отчёт лишь сообщает, что волна закрылась без этих пяти требований.
> Цитаты переносятся в следующую волну дословно, как они стоят здесь.

| ID | Из слов владельца (дословно) | Статус | Основание | Где |
|----|------------------------------|--------|-----------|-----|
| R27 | «16 типов документов водителя, 30 дней демо, 12 языков интерфейса… но у нас уже поменялось много чего. У нас уже есть и модуль водителей, модуль планирования, модуль ремонтов и будет дальше» | deferred | Решение владельца 17.08 на показе: «Карту сейчас → деплой → контент отдельной волной». Позиционирование, статистика и дорожная карта переписываются СЛЕДУЮЩЕЙ волной, в отдельной сессии: перепись затрагивает 12 локалей, а правка одной локали без остальных считается дефектом.  позиционирование лендинга отстало от продукта: страница продаёт справочник документов, а продукт стал системой из модулей | следующая волна (новая сессия) |
| R28 | «нам нужно дорожную карту обновить» | deferred | Решение владельца 17.08 на показе: «Карту сейчас → деплой → контент отдельной волной». Позиционирование, статистика и дорожная карта переписываются СЛЕДУЮЩЕЙ волной, в отдельной сессии: перепись затрагивает 12 локалей, а правка одной локали без остальных считается дефектом. Содержание дорожной карты: **черновик собираю я** по коду и базе знаний, владелец правит — его решение там же. раздел «Дорожная карта» на странице описывает не то, что уже выпущено | следующая волна (новая сессия) |
| R29 | «Каждый водитель готов всегда» | deferred | Решение владельца 17.08 на показе: «Карту сейчас → деплой → контент отдельной волной». Позиционирование, статистика и дорожная карта переписываются СЛЕДУЮЩЕЙ волной, в отдельной сессии: перепись затрагивает 12 локалей, а правка одной локали без остальных считается дефектом.  формулировка владельца, просится в обещание страницы | следующая волна (новая сессия) |
| R30 | «нужно тут больше про статистику рассказать… вы можете работать с помощью своего трекинга, но это приложение даёт вам обзор на всю вашу компанию, которая помогает вам планировать и прогнозировать, куда машины нужно везти, сколько водителей сейчас в отпуске, какие машины в ремонте» | deferred | Решение владельца 17.08 на показе: «Карту сейчас → деплой → контент отдельной волной». Позиционирование, статистика и дорожная карта переписываются СЛЕДУЮЩЕЙ волной, в отдельной сессии: перепись затрагивает 12 локалей, а правка одной локали без остальных считается дефектом.  ключевая мысль: не замена трекеру, а обзор и планирование поверх него | следующая волна (новая сессия) |
| R31 | «всё это удобно, красиво, защищённо, безопасно… есть определённый workflow, который не будет меняться, если у вас уйдёт сотрудница или ещё кто-то и заберёт с собой какие-то данные, знания или workflow. Здесь уже всё сделано по стандартам» | deferred | Решение владельца 17.08 на показе: «Карту сейчас → деплой → контент отдельной волной». Позиционирование, статистика и дорожная карта переписываются СЛЕДУЮЩЕЙ волной, в отдельной сессии: перепись затрагивает 12 локалей, а правка одной локали без остальных считается дефектом.  вторая ключевая мысль: процесс живёт в системе, а не в голове сотрудника — устойчивость к уходу людей | следующая волна (новая сессия) |
| R32 | «Я вижу карту, но она очень маленькая где-то внизу. Она даже не на фоне. Ну, это будет задний фон, ещё что-то, чтобы будет красиво ехать. А здесь-то вообще как-то иначе. Это нужно исправить» | done | Решение владельца 17.08 на показе: «Карту сейчас → деплой → контент отдельной волной». Это правка результата, а не новая работа, поэтому остаётся в текущей волне и идёт ПЕРЕД выкатом. **это правка результата таска 06**, а не новая идея: `R09`/`G03` просили «штуку, которая заставит задержаться», а мини-карта под текстом этого не делает | таск 11, коммит ниже. Закрыт ПО СУЩЕСТВУ слов, а не «карта стала больше»: поле на всю пиннутую сцену, карточки поверх, машина едет по полю. Оба ревьюера приняли отступление от буквы тикета — карта осталась одним экземпляром, `aria-label` цел (`R05.2`). ОСТАТОК ВЛАДЕЛЬЦУ: на шагах 2 и 3 силуэт проходит за непрозрачным передним планом — свойство композиции, решение его |

### Решения владельца на показе 18.08 (после таска 11)

| ID | Из слов владельца (дословно) | Статус | Основание | Где |
|----|------------------------------|--------|-----------|-----|
| R33 | «Подсветить активный участок, чтобы движение читалось и за карточкой» | done | Фраза состоит из **механизма** и **цели**. Механизм убил замер (`D07`: на шагах 2–3 у участка ноль видимых точек), замену выбрал владелец сам, с числом на руках, 18.08 — значит требование закрыто **по цели**. Подсвечивается внешняя кромка карточки: тёмная тема 4,192 / 4,246 против противоположной кромки 1,020 / 1,001; светлая 2,363 / 2,390 против 1,022 / 1,002. Направленность совпала с геометрией в **8 клетках из 8**. Содержимое карточки не тронуто — границу держит свойство примитива: внешняя тень отсекается по border-box. ОГОВОРКА ВЛАДЕЛЬЦУ: в светлой теме эффект вдвое слабее (2,36 против 4,19), а обе темы — приёмочное требование `R19i` | таск 13, коммит ниже |
| R34 | «Усилить карту в светлой теме» | done | контраст линии маршрута к локальному фону 1,289 → 1,832 (×1,42, медиана по 233 точкам на путях); тёмная не тронута; контраст текста шагов не сдвинулся, вуаль не уплотнялась | таск 12, коммит ниже |


Показ шёл на localhost, четыре развилки — все закрыты его словами.

| Развилка | Решение владельца | Следствие |
|---|---|---|
| На шагах 2 и 3 силуэт машины уходит за карточку | **«Подсветить активный участок, чтобы движение читалось и за карточкой»** | таск 12. Композицию не двигаем — карточки сцены остаются главным доказательством продукта |
| Карта в светлой теме бледная, читается водяным знаком | **«Усилить карту в светлой теме»** | таск 12. Усиление обнуляет доказательство читаемости («худший пиксель под текстом — сама канва»), поэтому контраст строится заново с негативными контролями |
| Фото Higgsfield видно только в раскадровке, на десктопе его нет | **«Оставить: на десктопе работает карта»** | остаток `R08` закрыт решением владельца, кредиты не тратятся (35 из 36). Два фона под одним текстом конкурировали бы |
| Барам доски не хватает ширины: на 320 подписи с многоточием, на широких скрыт счётчик заказов | **«Оставить до волны контента»** | содержимое мока — часть позиционирования, а его переписывает волна `R27`–`R31`. Сейчас честно: обрез виден как обрез, а не съедает буквы молча |

## Обнаруженное сборкой (`D##`) — не требование владельца, а то, что доказал код

| ID | Что доказала сборка | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| D01 | Текст выходит за карточку тарифа **на боевом сайте, до нашей работы**: `de` +8 px и `pl` +6 px на ширинах 1200 и 1440 | done | Доказательство «дефект старше нашей работы»: прежняя раскладка подставлена в тот же DOM — те же локали, те же значения. Полный замер 12 локалей × 4 ширины при колонке 221,5 px (это 1024) вскрыл шире: `de` +49, `pl` +47, `cs` +16,6, `it` +16,4, `lv` +11, `ru` +8. Виновники — `.cap-row` (flex без переноса) и `.tier-price` (слово «Индивидуально» 179,5 px в коробке 172). Чинится сейчас, решением оркестратора: оставлять дефект, задевающий половину языков, нельзя. После починки — ноль переполнений во всех 48 клетках | таск 01, лечение внутри карточки; сетка не тронута |
| D02 | Низ сцены истории переполнялся на высотах **861–899**: на `de` при 1440×880 — 8 px за край. Тот же вред, что в R03, на другой высоте, и существовавший **до** этой работы (компакт стоял на `max-height: 760`) | done | Найдено тем же приёмом, что D01 — прогоном по длинным локалям. Исполнитель таска 04 отказался чинить сам: число принадлежит общей шкале закоммиченного таска 01. Решение оркестратора: `--vh-compact` 860 → **900**; обрезанный низ на немецком — невыполненный R03, а цена (ужатый заголовок на 1440×900 у всех) дешевле. Владельцу сказано, откат в одну строку. Замер после: 1440×880 `de` −8,1 → **+99**; 1440×900 +1,1 → **+109**; 1440×901 негативный контроль — компакт выключен; 1024×768 и 1280×720 не сдвинулись | таск 04, `globals.css`; инвариант и обязанность перемерять — в спеке |
| D03 | Спецификация выставляла карте два пропа (`route`, `active`) — отношение «4 шага → 3 участка» ими не выражается: между городами машина обязана стоять в доле пути, а не в узле | done | Исполнитель таска 06 завёл третий проп `travelled` (0…1), геометрию оставил спрятанной; ревью спеки поймало расхождение как мягкое `Wrong`. Проверено: `journeyTravelAt(0,25) = 1/3` — на границе шага машина всегда в городе, а не посреди участка; узлов на маршруте четыре, участков три. Решение оркестратора: спека приведена в соответствие, шов остался один — требование R09 не двинулось | таск 06, `spec.md` «Границы и швы» |
| D04 | На вьюпорте 320 колонка `.scrolly-grid` растягивается по min-content первого кадра (`.dc-tabs` 320 → карточка 348) при содержимом страницы 310: **все четыре карточки истории режутся справа примерно на 56 px**. Дефект старше этой работы | done | Найден исполнителем таска 06 **вне своей зоны** и подтверждён кадром `320-light-step1.png`: обрывается ряд вкладок и строка «конфиденциальное — по ролям». Невидим замером страницы — горизонтальный скролл прячет `body`, поэтому заявленный исполнителем `docOverflowX = 0` не доказывал ничего; честный измеритель — `scrollWidth − clientWidth` по карточке И всем её потомкам, как в инварианте тарифов. Ни одна строка Б8 трек `.scrolly-grid` не называет, поэтому это класс `D01`/`D02`, а не потерянное требование. Решение оркестратора: **починить внутри этой волны** — бриф делает 320 шириной приёмки (`R07`), и принять волну с обрезом на ней нельзя | таск 08 (нарезан после ревью таска 06), коммит `f54c176`. Причина оказалась двойной: `1fr` = `minmax(auto,1fr)` И `margin: 0 auto`, отменяющий растяжение грид-элемента. Барьеров теперь два независимых, негативный контроль ставится парами — записано инвариантом в `interfaces.md` |
| D05 | На странице **две** доски с подписями дней, и правило переноса таска 08 легло только на доску сцены: доска **первого экрана** осталась `white-space: nowrap` и рубит подпись без многоточия. Замер приёмки (нужная ширина через `Range` при принудительном `nowrap`, не через `scrollWidth`): 320 — ячейка 28 px против 40,8 нужных ⇒ **−12,8 px** в `en es fr it pl ro` и −6,0 в `ru cs de lt lv uk`, обрез +13,0 и +6,0; 375 — **−1,8 px** в тех же шести; 1100 — минимум по странице **−3,4** (`pl` −3,3), обрез +3,0. На 430, 1024, 1200 и 1440 — норма | done | Дефект старше этой работы: таск 08 его не создал, он вылечил ОДНУ из двух досок, и приёмка это увидела числом. Класс тот же, что `D01`/`D02`/`D04` — молчаливая рубка текста, невидимая ни сборке, ни линтеру. Решение оркестратора: чинить, потому что 320 и 375 — ширины приёмки по брифу, а 1100 — обычный десктоп. **Оговорка:** атрибуция −3,4 на 1100 именно к доске первого экрана — вывод приёмки, а не прямой замер (она единственная с `nowrap`); таск 09 обязан это проверить замером. **Риск лечения:** доска первого экрана — самая деликатная часть страницы (наклон, контейнерный запрос 560, инвариант «два ребёнка у `.hero-grid`»), перенос там может вырастить высоту над фолдом | таск 09, `hero.css` + снятие дубля в `mobile.css`, коммит `ebcfa34`. Атрибуция проверена прямым замером: честный дефицит на 1100 был **−1,8**, а не −3,4 — приёмка смешала трансформированный rect с layout-числом и мерила без 10 px полосы прокрутки. Инвариант держит ОДНО правило на обе доски |
| D06 | Чипы документов в доске **первого экрана** рубятся без многоточия: `.hero-board .dtags` теряет **11…41 px** во всех 12 локалях при ширинах ≥ 1024 и 1…14 px на 320–430. Видно на кадре 1100: чип читается «VIS · 30» вместо полного «VIS · 30 dni». Дефект старше этой работы | done | Найдено исполнителем таска 09, который сознательно НЕ стал сужать колонку водителя: забирать ширину у уже режущего блока значило углубить чужой обрез. Проходит все три условия правила границы: (а) виден в приёмочном кадре на ширине из брифа; (б) лечится в файле, который волна уже правит; (в) лечение структурное — номер машины **дублируется** полосой рейса той же строки (`Hero.tsx:224-225`, `:247`), значит скрытие дубля ничего не теряет. Тот же приём, которым таск 08 закрыл дубль в доске сцены через `.spz`. Отличие в цене: там обрез жил ниже 768 и правка была мобильной, здесь — флагманский первый экран на десктопе, поэтому нужен свой прогон кадров | таск 10, коммит ниже. Закрыто ДВУМЯ барьерами: снятие дубля номера и видимое многоточие. Попутно закрыты две слепоты измерителя: ноль по `.dtags` оказался нулём по построению, а полосы без номера не мерились вообще — под ними жили 134 молчаливых обреза. Правка вышла в `Hero.tsx`, `Scrolly.tsx`, `Europe.tsx`: голый текстовый узел во флексе — анонимный флекс-элемент, `text-overflow` к нему не применяется |
| D07 | **Посылка «движение будет читаться самим маршрутом» неверна для середины маршрута.** Владелец выбрал: «Подсветить активный участок, чтобы движение читалось и за карточкой» (`R33`). Подсветка сделана и работает — снятие вуали поднимает контраст того же участка с **1,363 до 3,965** в тёмной и с 1,484 до 2,439 в светлой. Но на шаге 2 у активного участка **НОЛЬ видимых точек**: линия и оба её города (MUC, LYO) целиком за карточкой сцены и за вуалью колонки шагов, а полоса зазора 32 px разницы не даёт (1,750 против 1,782 тёмная, 2,139 против 2,130 светлая) | актуально, следствие снято | Ошибка в формулировке тикета, а не в исполнении: «движение читается самим маршрутом» написано без проверки, остаётся ли маршрут виден. Проверять надо было до, а не после. Границы, установленные ревью: **сдвинуть кадр карты — починка** (проекция цела, смысл не меняется); **сдвинуть города — запрещено вообще**, подвинутый ради красоты Лион это выдуманная география в продукте, который продаёт точность; **луч поверх карточки — незаконен** (карточка существует ради `G04` и истории 21 «я узнаю наш продукт, а не абстрактные плашки», а луч поверх мока обнуляет замеры `D05`/`D06` в 12 локалях × 2 темы со молчаливым провалом); **свечение по внешней кромке карточки — законно**, мерить надо только контраст кромки | **Обход:** `R33` закрыт кромкой карточки (таск 13). Само доказанное остаётся правдой — участок на шагах 2–3 по-прежнему невидим, и это выстрелит при первой же смене кропа, ширины карточки или маршрута. Строка переносится в манифест следующей волны: контентная волна двигает ровно эти тексты и карточки |
| D08 | **Прошлый детектор переполнения не мог покраснеть.** `body { overflow-x: hidden }` делает правило «переполнение оправдано, если выше есть режущий предок» оправдывающим ВСЁ: мутация `min-width: 520px` оставила документный замер **0**, пока контент уезжал за экран на **228 px**. Заменён метриками, которые краснеть умеют. На них найден настоящий дефект: `de` @ 320, секция видео — `clientWidth` 310 против `scrollWidth` 356, **срезано 46 px**, прокрутить нельзя. Причина — неразрывное `Transportunternehmen.` (21 символ). Ни в одной из 11 остальных локалей, ни на одной другой ширине | done | Четвёртый лживый ноль за прогон и первый — в самом инструменте приёмки. Дефект проходит все три условия правила границы: виден в приёмочном кадре на ширине из брифа, лечится в наших файлах, лечение структурное (перенос длинного слова ничего не удаляет) | таск 14, коммит ниже. Лечение — ОДНО правило `overflow-wrap: anywhere` на заголовки секций, без порога по окну. 960 замеров: отличие от состояния «правило снято» РОВНО ОДНО, тот самый `de`@320 (2 → 4 строки), остальные 959 бит в бит |

> **🔴 Цена решения D02, которую надо помнить.** Число 900 выведено из точки обрыва:
> запас полноразмерной стороны у нижнего края ≈ **+2 px** против **+109** у компактной.
> Любой длинный перевод, тринадцатая локаль или подросший кегль вернут переполнение
> в полосу 901–940 молча. Первый экран и тарифы защищены содержимым, сцена — числом;
> это единственное место работы, которое стареет от текста.

> **Оговорка к главному утверждению прогона.** «1200 и 1440 байт в байт с состоянием
> до таска 01» после этой починки для `de` и `pl` **намеренно неверно** — там было
> переполнение, теперь его нет. Без этой записи следующий читатель прочтёт расхождение
> как регрессию.

## Возникшее в брифинге (на словах пользователя)

| ID | Слова пользователя | Статус | Основание | Где |
|----|--------------------|--------|-----------|-----|
| G01 | «подключись к нему… Install the CLI… Authenticate… Install the companion skills… let me know when it's ready» | done | 17.08: CLI 1.1.23, 8 skills Higgsfield, вход выполнен, баланс 36 кредитов | — |
| G02 | «могу купить подписку за 20 долларов, дай знать если надо» | done | решение отложено: стартуем на бесплатных 36 кредитах, сигнал владельцу при остатке < 15 | spec, история 38 → таск 06, коммит `84f9688` |
| G03 | «там есть раздел „история одного водителя“ вот туда можно что-то интересное сделать» | done | секция = `Scrolly.tsx`, та же, что чинится по R03. Решение: маршрут по карте идёт синхронно с 4 шагами истории | spec, история 19 → таск 06, коммит `84f9688` |
| G04 | «точно хочу чтобы были использованы элементы нашего приложения» | done | эталон записан в `reference.md`; сайтов-ориентиров пользователь не назвал («пока не знаю»). Элементы интерфейса воспроизводятся кодом в стиле продукта, на демо-данных — не на данных клиента | spec, истории 21–22 → таск 06, коммит `84f9688` |
| G06 | «Перенести на ступень 980» (о полосе 1200–1279, где после укрупнения шкалы первый экран стал одноколоночным, а тарифы — 2 колонки вместо 4) | done | принятое расхождение: пороги 1060/1100 сняты, но правила hero и тарифов переезжают на ступень `l`, а не на `xl`. Пятый порог не заводится, литерал в JS не возвращается | таск 01, второй дозапрос |
| G05 | «делаешь всё ты, только показываешь результат на локалхост и после апрува делаешь деплой и паблиш на продакшн» | in-spec | порядок выката: ветка → таски → локальный показ → апрув владельца → merge в `main` → авто-деплой Vercel | spec, история 40 |

---

## Гейт G3 — пройден 17.08

**Вперёд:** 21 требование разложено по 7 таскам, номер приписан в колонке «Где».
Семь строк остались `in-spec` осознанно и не потеряны: R13–R18 — это оценка самого skill,
её закрывает отчёт фазы 8, а не код; G05 — порядок выката, его первая половина (показ
на localhost) живёт в таске 07, вторая (merge и деплой) исполняется после апрува владельца.

**Назад:** каждый из 7 тасков ссылается минимум на одно требование. Тасков без родителя нет.

**Полнота:** у каждого таска есть зона и волна. В одной волне зоны не пересекаются:
волна 1 — стили против видео-секции; волна 2 — шапка против скроллителлинга против карты,
разные файлы стилей; волны 3 и 4 — по одному таску.

## Гейт G2 — пройден 17.08

Своя половина: `open` — ноль, 28 строк переведены в `in-spec` с указанием раздела.
Независимая половина: субагент, получивший **только** бриф и спецификацию, вернул
**8 пропусков, 4 половинчатых места и 20 пунктов «этого не было в брифе»**.
Из последних 19 признаны законным углублением (`R##.n` и `G##` — режим deep и ответы
брифинга, которых проверяющий не видел), один вырезан как незаказанный:
`R01.3` — отступ первого экрана от фактической высоты шапки.
Крупнейшая поимка: требования обкатки R13–R18 существовали в спеке одной строкой,
а граница «репозиторий Lovable не трогать» не была записана нигде.

## Гейт G1 — пройден 17.08

Брифинг закрыт: 6 вопросов, все — настоящие развилки, ни одного процессного.
Строки, оставшиеся `open`, вопроса не требовали — это находки Б8 с однозначным лечением;
их переведёт в `in-spec` гейт G2. Заблокированных неизвестных не осталось:
доступ к Higgsfield получен, бюджет назван, порядок выката согласован.

| R23i | *(подразумевается)* где именно на странице живёт новая графика — какая секция, что она вытесняет | done | владелец указал секцию: «история одного водителя». Большая карта Европы остаётся своей секцией, ничего не вытесняется | spec, Решения §5 → таск 06, коммит `84f9688` |
| R24i | *(подразумевается)* «задержаться на сайте» измеряется чем-то — иначе требование непроверяемо | placeholder | решено мной (крафт): события шагов истории в GA4 через уже стоящий GTM. **Код закрыт таском 06** — `journey_step` уходит в `dataLayer` по одному разу на шаг. Остаток НЕ кодовый: в контейнере GTM нет тега на это событие, поэтому доля дочитавших до 4-го шага в GA4 пока не появится. Одна настройка в консоли владельца, репозиторием не закрывается | spec, история 28 → таск 06 |
| R25i | *(подразумевается)* попадёт ли работа в прод: PR, merge, авто-деплой — необратимое действие, гейт во всех режимах | closed | пользователь: «делаешь всё ты, только показываешь результат на локалхост и после апрува делаешь деплой и паблиш на продакшн». Гейт остаётся: без апрува на локалхосте деплоя нет | — |
| R26i | *(подразумевается)* правка CTA (R02) затрагивает все 12 локалей, а не только русскую | done | тексты локалей не трогаем: выбор варианта переезжает в код | spec, истории 10–14 → таск 03, коммит `670ded1` |
